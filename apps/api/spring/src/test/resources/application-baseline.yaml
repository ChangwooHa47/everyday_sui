spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:everyday_baseline;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    properties:
      hibernate:
        default_schema: PUBLIC
    hibernate:
      ddl-auto: create-drop
    show-sql: false
server:
  address: 127.0.0.1
  port: 18080
jwt:
  access-token-expiration-ms: 86400000
  secret: everyday-baseline-only-signing-key-never-for-production-2026
app:
  llm:
    api-key: ""
  image:
    api-key: ""
    api-secret: ""
  cors:
    allowed-origins: http://127.0.0.1:13000,http://localhost:13000
